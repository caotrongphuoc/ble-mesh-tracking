# ESP-IDF build output
build/
managed_components/
sdkconfig.old
dependencies.lock

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
~$*.docx
.~lock.*#

# Accidental drag-and-drop uploads
*.zip
*.docx

# Personal review / defense notes kept alongside the repo
ON_TAP*.txt

# Local deployment notes with real secrets
deploy-config.txt

# Cryptographic material — every deployment generates its own.
# See keys/README.md, thingsboard/tls/gen_certs.sh
keys/*.pem
thingsboard/tls/ca.key
thingsboard/tls/ca.pem
thingsboard/tls/ca.srl
thingsboard/tls/server.csr
thingsboard/tls/server.key
thingsboard/tls/server.pem
apps/gateway/components/bmt_mqtt/ca.pem
components/bmt_ota/ota_ca.pem

# OTA payloads — build locally, CMake POST_BUILD copies them here
firmware/*.bin
# nRF52840 Beacon signed images — build locally with make_uf2.ps1
*.uf2
*.hex
__pycache__/
*.pyc
